// Im thinking we make an /admin route that just has a prompt which then reaches to the admin users
// collection on mongodb which then will have a basic add payment feature ui and add a token to the
// requests so that it can be authenticated.
